'use client';

/**
 * Page transition wrapper.
 * Placeholder for Framer Motion AnimatePresence integration.
 *
 * TODO: Implement page transition animations when Framer Motion is added.
 */

import type { ReactNode } from 'react';

interface PageTransitionProps {
  children: ReactNode;
}

export default function PageTransition({ children }: PageTransitionProps) {
  // Animation logic will be added when Framer Motion is installed.
  return <>{children}</>;
}
